[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "agent-supply-trust"
version = "1.0.0"
description = "Evaluate packages, containers, MCP servers, agent skills and plugins for provenance, permissions and runtime risk."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "API Disk Integrations" }]
keywords = ["api", "sdk", "agent-supply-trust"]
classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Software Development :: Libraries",
]
dependencies = []

[tool.hatch.build]
ignore-vcs = true

[project.urls]
Homepage = "https://agentsupplytrust-api.com/"
Documentation = "https://agentsupplytrust-api.com/#docs"
Pricing = "https://agentsupplytrust-api.com/#pricing"
Privacy = "https://agentsupplytrust-api.com/privacy"
Terms = "https://agentsupplytrust-api.com/terms"
Source = "https://github.com/API-Disk-Integrations/agent-supply-trust"
Issues = "https://github.com/API-Disk-Integrations/agent-supply-trust/issues"
Changelog = "https://github.com/API-Disk-Integrations/agent-supply-trust/blob/main/CHANGELOG.md"

[tool.hatch.build.targets.wheel]
packages = ["src/supply_chain_trust"]

[tool.hatch.build.targets.sdist]
include = [
  "/LICENSE",
  "/README.md",
  "/pyproject.toml",
  "/src/supply_chain_trust/__init__.py",
]
